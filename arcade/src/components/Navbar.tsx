import React from 'react';

interface NavbarProps {
    
}

const Navbar: React.FC<NavbarProps> = () => {
    return (
        <div>
            <p>Navigation</p>
        </div>
    );
};

export default Navbar;